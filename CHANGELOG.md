# 변경 내역

## 다음 판

### 성능

- 게임이 전반적으로 빨라졌습니다. 같은 장면에서 같은 화면을 똑같이 그리면서 에뮬레이터가 쓰는 시간이 LGT는 약 30%, KTF는 5~12% 줄었습니다. 버벅이던 게임은 그만큼 여유가 생기고, 원래 잘 돌던 게임은 CPU를 덜 씁니다
- LGT 게임이 그림을 한 번 그릴 때마다 화면 한 장을 통째로 두 번씩 베끼면서 그 값을 한 픽셀씩 다시 조립하고 있던 것을 없앴습니다 — 20초 장면 하나가 8.6GB를 쓰고 버리던 자리입니다. 특히 심하던 몇 종은 부팅 구간이 9배까지 빨라졌습니다
- 게임이 메모리를 읽고 쓸 때 최근에 쓴 페이지를 하나만 기억하던 것을 256개로 늘렸습니다 — 게임은 두 영역을 번갈아 읽는데(그림 원본과 화면), 하나만 기억하면 매번 빗나갑니다
- 바뀐 화면은 한 픽셀도 없습니다. 성능을 잰 구간의 886장 전부가 이전 판과 완전히 같은 그림이고, 게임이 실행한 명령 수도 한 개까지 같습니다

### LGT

- 실행되지 않던 게임 몇 종이 실행됨 — 게임 안의 클래스 기록을 읽는 자리가 한 칸 어긋나 있어서, 필드가 없는 클래스는 메서드가 하나도 없는 것처럼 보이던 문제. 시작 클래스의 생성자를 못 찾거나 스레드에 `run`이 없다며 멈추던 게임들이 여기에 걸려 있었습니다
- 게임이 자기 로그를 한 줄 찍는 순간 죽던 문제 수정 — `System.out`이 비어 있었습니다
- 글꼴 지정·문자열 자르기·파일 존재 확인 등 게임이 부르는데 없던 기능 몇 가지 추가
- 텍스트 파일을 통째로 읽어 길이를 재는 게임이 시작하자마자 멈추던 문제 수정
- 화면 조작 함수 자리에 게임이 남겨 둔 엉뚱한 값을 그대로 실행하다 죽던 문제 수정 — 1종이 3000틱을 끝까지 갑니다
- 파일 이름 바꾸기(`MC_fsRename`) 추가 — 저장을 임시 이름으로 쓴 뒤 바꿔 끼우는 게임이 여기서 멈춰 "재부팅해 달라"는 화면을 띄우고 있었습니다
- 실행되지 않던 게임 3종이 추가로 실행되고, 화면이 검은 채로 끝나던 게임 4종이 그림을 그립니다. **테스트 세트의 LGT 게임 94종이 전부 구동합니다**
- 시작조차 못 하던 게임 1종 실행됨 — 게임 파일이 시작 위치를 두 군데에 적어 두는데, 우리가 읽던 쪽이 틀린 값을 담고 있었습니다
- 게임이 화면에 올려 달라고 건넨 그림을 무시하고 늘 화면 자체만 올리던 문제 수정 — 자기 그림에 한 장을 다 그린 뒤 화면에 얹는 게임은 아무것도 보이지 않았습니다
- **화면에 보이는 것이 "마지막으로 올린 그림"이 되도록 고쳤습니다** — 예전에는 물어보는 순간의 그리기 도중 상태를 보여 줘서, 화면을 채우고 올린 뒤 곧바로 지우고 다음 장을 그리는 게임은 늘 지운 화면만 보였습니다. 게임 3종이 여기 걸려 있었습니다
- 글자 폭을 잴 때 게임이 알려준 글자 수를 무시하던 문제 수정 — 줄바꿈 자리를 한 글자씩 늘려 가며 찾는 게임이 늘 같은 답을 받아 영원히 끝나지 않았습니다
- 인터페이스를 통한 메서드 호출이 없어 시작 직후 멈추던 게임 1종 실행됨
- 소리 클립을 채우고 비우는 기능, 글자열 일부 그리기, 키를 방향으로 바꿔 주는 기능 추가

### KTF

- 게임이 자기 디렉터리를 만드는 호출이 없어서 시작 화면도 못 보던 2종 실행됨
- 실행되지 않던 게임 10종이 실행됨. 원인은 셋이고, 하나가 다섯 종을 붙잡고 있었습니다
- 게임이 오류를 붙잡았을 때 무엇을 붙잡았는지 전해 주지 않던 문제 수정 — 게임은 그 자리에 남아 있던 엉뚱한 값을 오류로 알고 다시 던지거나 화면에 찍으려다 죽었습니다. 세이브가 아직 없는 첫 실행에서 특히 자주 걸리던 자리입니다
- 게임이 직접 오류를 던지는 길이 아예 없던 문제 수정
- 그림 파일을 두 번 읽는 게임이 그림을 통째로 잃던 문제 수정 — 읽던 자리를 처음으로 되돌리는 기능이 없어서, 게임은 자기 디코더가 낼 리 없는 오류를 붙잡고 빈 그림을 그리고 있었습니다 (세 플랫폼 공통)
- 화면 갱신을 자기 순번으로 예약해 돌리는 게임이 400틱 동안 아무것도 그리지 못하던 문제 수정
- 실행되지 않던 게임 6종이 추가로 실행됨
- 같은 미들웨어를 쓰는 2종이 첫 프레임에서 죽던 문제 수정 — 게임이 쓰라고 준 공간이 우리가 읽는 칸까지만 잡혀 있어서, 게임이 그 뒤에 쓴 값이 바로 그 공간을 가리키는 포인터를 덮어쓰고 있었습니다
- 게임이 받는 64비트 값(시각 등)이 절반만 전달되던 문제 수정 — 시각을 문자열로 만들어 자리수를 세는 게임이 시작하자마자 멈추던 자리입니다
- 오전/오후를 읽는 게임이 시작하자마자 멈추던 문제 수정 (세 플랫폼 공통)
- 화면 픽셀을 단말 형식 그대로 읽고 쓰는 기능 추가 — 상단 표시줄을 직접 그리는 게임이 부릅니다
- 시작할 때 그림을 통째로 푸는 게임 2종이 명령 한도에 걸려 시작조차 못 하던 문제 수정 — 한도가 폭주를 막는 게 아니라 로더를 자르고 있었습니다
- 실행되지 않던 게임 4종이 추가로 실행됨
- 게임이 오류를 붙잡고 원래 자리로 돌아갈 때, 그 자리가 이미 지나온 곳이면 엉뚱한 데서 되돌려 놓던 문제 수정 — 첫 화면을 그리다 죽던 게임이 여기 걸려 있었습니다
- 오류를 붙잡은 게임이 방금 빠져나온 자리에서 같은 오류를 다시 붙잡아, 자기 오류 처리 코드만 끝없이 되풀이하던 문제 수정
- 게임에 알려주는 메모리 크기가 실제 단말보다 훨씬 커서, 남은 용량을 백분율로 계산하는 게임이 음수를 읽고 계속 기다리기만 하던 문제 수정
- 저장소가 아직 없는데도 있다고 답하던 문제 수정 — 처음 실행이라는 것을 알아야 새로 만드는 게임들이 첫 실행 경로를 못 만나고 있었습니다
- 게임이 자기 클래스 이름을 물었을 때 그런 클래스가 없다고 답하던 문제 수정 — 한 종이 아무것도 그리지 못하고 빈 화면만 되풀이하던 자리입니다 (SKT 공통)
- 게임이 `System.out`으로 찍는 자기 기록이 버려지던 것을 로그로 남김 — 멈춘 게임의 원인을 게임 자신이 말해 주는 자리입니다
- 실행되지 않던 게임 4종이 추가로 실행됨
- 화면 전체를 그리려고 클리핑을 끄는 게임이 첫 화면에서 죽던 문제 수정
- 그림을 자기 코드로 직접 들여다보는 게임이 첫 프레임에서 죽던 문제 수정 — 그림이 담고 있는 정보의 배치가 실제 단말과 달랐습니다
- 단말 문자표(EUC-KR)로 저장한 글자를 되읽는 게임이 아무것도 그리지 못하던 문제 수정 — 같은 일을 하는 세 가지 방법 중 하나만 이 문자표를 거절하고 있었습니다 (세 플랫폼 공통)
- 게임이 붙잡은 오류를 화면 대신 로그로 찍어 주도록 함 — 위 문제를 찾아낸 자리입니다
- 이름을 입력받는 대화상자(`com/ktf/kfc`)를 쓰는 게임 1종 실행됨
- 타이머 안에서 죽은 게임의 진단 정보가 비어 있던 것을 채움
- 자기 화면을 스스로 그리고 올리는 게임 위에 우리가 빈 화면을 덧그리던 문제 수정 — 1종이 흰 화면만 보이다가 제 타이틀 화면을 그립니다. 게임이 직전 장면을 스스로 올렸으면 그 차례는 건너뜁니다

### 그림

- 게임이 색을 바꿔 가며 쓰는 그림 파일을 못 읽던 문제 수정 — 원래 단말은 검사하지 않던 검사값 때문에 그림 하나가 통째로 거절되고 있었습니다 (세 플랫폼 공통)
- 같은 그림 파일이 검사값을 하나 더 갖고 있어서 여전히 거절되던 문제 수정 — 압축된 그림 안쪽의 검사값도 원래 단말은 보지 않습니다 (세 플랫폼 공통)

### 게임 추가

- 에피소드 세 개를 하나로 묶어 둔 zip을 올리면, 게임이 아니라 게임 세 개를 담은 묶음이라고 알려 줍니다 — 풀어서 하나씩 올리면 세 개 모두 실행됩니다

## 0.2.2 — 2026-08-23

### 입력

- 키패드에 메뉴 키 추가 — 이 시절 게임이 인게임 메뉴를 걸어 두던 그 키
- 키패드 종류 고르기가 설정 패널로 이동하고, 고른 값이 기억됨
- 화면을 끌었을 때 게임에 전해지는 종류가 잘못되어 있던 것을 수정

### 저장

- 저장 도중 전원이 꺼지거나 디스크가 차도 세이브가 반만 남지 않도록, 통째로 바꿔 끼우는 방식으로 변경
- 같은 게임을 두 곳에서 열면 나중에 연 쪽을 거절 — 두 화면이 같은 세이브에 번갈아 덮어써 진행이 사라지던 문제
- 게임이 열려 있는 동안에는 저장 API가 그 게임의 세이브를 건드리지 않음

### 안정성

- 게임을 끝낼 때 드물게 서버가 함께 죽던 문제 수정
- 그림 파일이 망가진 게임이 서버를 통째로 멈출 수 있던 문제 수정 — 같은 서버에 접속한 다른 사람의 게임까지 함께 끊기던 경로

### SKT

- 화면 크기를 밝히지 않는 게임이 자기 리소스 이름으로 알려 주는 크기를 읽어, 로고만 띄운 채 멈추던 게임이 실행됨
- 게임이 부르는데 없던 자바 표준 기능 몇 가지 추가

### 오프라인

- 인터넷이 끊긴 상태에서 페이지를 열면 파일 두 개가 없어 실패하던 문제 수정

### 릴리스 빌드

- 릴리스에서는 브라우저 쪽 로그를 모으지도, 서버로 보내지도 않음
- 디버그 빌드가 남기는 로그에 개수·용량·보관 기간 상한 추가

### 기타

- 저장소에서 직접 빌드하려면 Go 1.25 이상이 필요합니다

## 0.2.1 — 2026-08-21

### 입력

- 키보드 꾹 누르면 연사 누른 것 처럼 작용하는 문제 해결
- 방향키 놓을 시 캐릭터가 그 자리에 멈춰 서던 문제 수정
- 키를 꾹 누를때 게임에 전해지는 반복 속도 조정
- 게임 속도 변경 시 반복도 같은 배율로 조정

### SKT

- `java.util.Timer` 구현

### 속도

- 서버가 게임을 돌리는 속도가 전반적으로 약 12% 상승

## 0.2.0 — 2026-08-19

### 구형 KTF 게임 지원 (BREW)

### 사운드

- KTF 게임 중 소리를 내는 방식이 다른 게임에서 아예 소리가 나지 않던 문제 수정
- 슬라이드 바에 따른 자연스러운 음량 처리 추가

### SKT

- 게임이 쓰는 클래스와 벤더 기능 구현
- 게임 안에 한글 파일명이 있으면 아예 열리지 않던 오류 수정
- SKT 게임에서도 치트 패널 사용 가능(쓰기 감시는 미지원)
- 찾은 주소가 어느 클래스의 값인지 목록에 표시됨

### 메모리 누수

- 화면을 매 프레임 새로 만들어 그리는 게임에서 화면이 반납되지 않음
- 이미지를 만들 때 쓰고 버리는 메모리가 조금씩 남음
- 오래 플레이할수록 조금씩 느려지던 것도 같은 원인

### 화면 크기 선택

- 설정에 화면 크기 항목 추가(240×320 기본, 176×220, 128×160, 240×400, 320×480)
- 작은 화면용으로 나온 게임이 기본 화면에서는 로고만 띄운 채 멈추던 문제 해결

### 그림이 깨지던 현상

- 캐릭터와 물체 주변이 분홍색 사각형으로 덮여 보이던 현상 수정
- 게임이 자기 글꼴로 그리는 한글이 보이지 않던 현상 수정

### 속도

- 화면을 직접 갱신하는 방식의 게임에서 CPU 사용량 약 20% 감소
- 그림을 많이 그리는 게임에서 전투처럼 복잡한 장면이 느려지던 것을 개선
- 휴대폰으로 접속해 화면이 복잡한 장면을 플레이하면 게임이 느려지던 문제 수정
- 휴대폰이 신호가 끊긴 채로 응답하지 않으면 한참 뒤에야 정리되던 것을 즉시 정리하도록 변경

### 진단

- 세션 보고서와 로그에 실제 진행 속도와 초당 처리 횟수가 표시됨
- 게임이 느릴 때 서버가 부족한 것인지 연결이 느린 것인지 구분하는 데 사용

### 기타

- 드래그하여 연속으로 키패드 누르는 기능 추가

## 0.1.4 — 2026-08-17

글자 윗부분이 잘리던 현상 핫픽스

### 글자 잘림

- 메뉴나 대화 상자처럼 칸이 좁은 곳에서 글자 윗줄이 잘리던 현상 수정
- 같은 원인으로 여러 줄 안내 문구가 붙어 보이던 것도 해결

## 0.1.3 — 2026-08-17

키패드 레이아웃 추가 및 코드 정비

### 키패드 Type3 레이아웃

- Type3 레이아웃 추가

### support.md

- 지원 게임 목록(`docs/support.md`)을 공개하고 README에서 링크

### 코드 정비

- 원호와 둥근 모서리 사각형 그리기를 세 플랫폼 모두에서 지원
- 단말기 자체 비트맵 형식(`LBMP`)을 읽음
- 폴더 하나로 감싼 게임 파일도 인식
- 벤더 폴더 없이 `games/`에 바로 넣은 게임도 목록에 표시
- 치트 창이 값을 바꾼 주체가 게임인지 에뮬레이터인지 구분해 표시

## 0.1.2 — 2026-08-16

Go 포팅 및 오류 수정

### 종료할 때 저장 중인 내용 유지 

- 멈추라는 요청을 받으면 저장을 마무리한 뒤에 종료
- lsof·ss·netstat 제거

### 시작할 때 빈 브라우저가 열리는 현상

- 서버가 실제로 응답하면 페이지를 열도록 수정

### 상태 확인

- 윈도우에서도 접속할 주소를 알려줌
- macOS 에서 유선·외장 랜을 쓰는 경우에도 그 주소를 찾음

### 포팅

- 표준 클래스 라이브러리 일부 Go 포팅
- 스크립트에 운영체제 별 세 벌씩 있던 처리를 서버 안에서 처리하도록 수정

## 0.1.1 — 2026-08-16

LGT Java 게임 성능 개선

### 화면 전체 복사 제거

- 그리기 호출 하나마다 화면 전체(240×320, 150KiB)를 메모리에서 읽고 다시 쓰던 것을 제거
- 화면과 게임 메모리는 이제 프레임 경계에서 한 번만 맞춤

### 프레임 간격 밀림 완화

- 프레임 끝에서 50밀리초를 더 먹던 원인 수정
  - 깨어날 시각을 보고 그 시각까지만 시계를 넘김
  - 실제로 다른 흐름이 있을 때만 차례를 반납

### 네트워크 접속 시도 시 세션 째로 죽는 문제

- 소켓을 만드는 호출(`org.kwis.msf.io.URL.find`)이 구현돼 있지 않아 발생
- 명세가 정한 예외(`SchemeNotFoundException`)를 돌려주어 게임 자체 안내를 띄우고 진행

## 0.1.0 — 2026-08-15

첫 공개 릴리즈입니다.

옛날 피처폰 게임을 PC에서 돌리고 브라우저로 보는 에뮬레이터입니다.
게임은 서버에서 돌고 브라우저는 화면만 받습니다. 같은 와이파이의 폰에서
접속하면 폰에서도 그대로 플레이할 수 있고, "홈 화면에 추가"로 앱처럼 쓸 수
있습니다.

**지원 플랫폼** — KTF·LGT의 WIPI 게임과 SKT 게임. 게임 54종의 구동을
확인했고, 그중 52종은 실제 플레이 화면까지 도달합니다. LGT 게임 중
Clet이 아닌 Java 앱 4종도 포함됩니다.

**들어 있는 것**

- 게임 목록 화면과 브라우저 키패드. 키보드 매핑은 키마다 바꿀 수 있고,
  키패드 배치는 Type1/Type2 두 가지입니다.
- 세이브. 게임 안에서 저장한 것이 서버에 남고 다음 실행에서 이어집니다.
  debug 빌드와 release 빌드는 세이브를 따로 씁니다.
- 배경음·효과음, 화질(원본·hq2x·hq3x·hq4x), 속도(0.25~4배) 조절.
- 메모리 검색 치트 도구. 값으로 주소를 좁혀 찾고, 고정하고, 어떤 코드가 그
  값을 쓰는지 감시할 수 있습니다.
- 서버 하나에 화면·글꼴·클라이언트가 모두 들어 있습니다. 실행 파일 하나와
  게임을 넣을 폴더가 전부이고, 설치 프로그램은 없습니다.

**받는 것** — 운영체제별 아카이브 5종(macOS arm64·x86-64, Linux
x86-64·arm64, Windows x86-64)입니다. 압축을 풀고 `games/` 폴더에 게임을 넣은
뒤 실행 파일을 더블클릭합니다.

| | 실행 | 멈추기 |
|---|---|---|
| Windows | `start.bat` | `stop.bat` |
| macOS | `start.command` | `stop.command` |
| Linux | `./start.sh` | `./stop.sh` |

`SHA256SUMS`로 받은 파일을 확인할 수 있습니다. **게임 파일은 들어 있지
않습니다.**

**처음 한 번 막히는 곳** — 서명된 앱이 아니라서 macOS는 "확인되지 않은
개발자", Windows는 SmartScreen 화면이 뜹니다. 아카이브 안 `README.txt`에
운영체제별로 넘어가는 방법이 적혀 있습니다. macOS는 `start.command`를
Control-클릭 → [열기]입니다.

**알려진 한계**

- 개발 중인 에뮬레이터입니다. 목록에 없는 게임은 대부분 돌지 않습니다.
- 네트워크를 쓰는 게임은 그 지점에서 더 진행되지 않습니다.
- 접속에 암호가 없습니다. 서버는 기본적으로 모든 인터페이스에 열리므로,
  같은 네트워크에 있는 사람은 누구나 게임 목록을 보고 세이브를 읽고 쓸 수
  있습니다. 이 컴퓨터에서만 쓰려면 `-addr 127.0.0.1:11541`로 띄웁니다.
- 첫 실행이 오래 걸리는 게임이 있습니다. 게임이 자기 데이터를 통째로 읽고
  DRM 검사를 하는 구간이라 원래 단말에서도 걸리던 시간입니다.

MIT 라이선스입니다. 함께 담긴 구성 요소의 고지는 아카이브 안
`THIRD-PARTY-NOTICES.md`에 있고, 서버를 띄운 뒤 `/licenses`로도 볼 수
있습니다.
