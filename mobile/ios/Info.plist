<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>
    <string>W-Feature</string>
    <key>CFBundleDisplayName</key>
    <string>W-Feature</string>
    <key>CFBundleIdentifier</key>
    <string>com.movingwoo.wfeature</string>
    <key>CFBundleExecutable</key>
    <string>wfeature</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>__VERSION__</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>MinimumOSVersion</key>
    <string>15.0</string>
    <!-- Everything below is what Xcode stamps and a plist written by hand does
         not. It is not decoration: a re-signing service reads
         CFBundleSupportedPlatforms to decide this is an iOS app at all, and an
         IPA without it is one such a tool can refuse before it ever looks at
         the signature. The DT keys are the same metadata by another name, and
         the build substitutes the SDK it actually used. -->
    <key>CFBundleSupportedPlatforms</key>
    <array>
        <string>iPhoneOS</string>
    </array>
    <key>DTPlatformName</key>
    <string>iphoneos</string>
    <key>DTSDKName</key>
    <string>iphoneos__SDK__</string>
    <key>DTPlatformVersion</key>
    <string>__SDK__</string>
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
    </array>
    <!-- The icon was a loose file with nothing pointing at it, which is why the
         home screen showed a blank square. These are the names the build
         resizes it into. -->
    <key>CFBundleIcons</key>
    <dict>
        <key>CFBundlePrimaryIcon</key>
        <dict>
            <key>CFBundleIconFiles</key>
            <array>
                <string>AppIcon60x60</string>
            </array>
            <key>CFBundleIconName</key>
            <string>AppIcon</string>
        </dict>
    </dict>
    <key>CFBundleIcons~ipad</key>
    <dict>
        <key>CFBundlePrimaryIcon</key>
        <dict>
            <key>CFBundleIconFiles</key>
            <array>
                <string>AppIcon60x60</string>
                <string>AppIcon76x76</string>
            </array>
            <key>CFBundleIconName</key>
            <string>AppIcon</string>
        </dict>
    </dict>
    <key>UIDeviceFamily</key>
    <array>
        <integer>1</integer>
        <integer>2</integer>
    </array>
    <!-- Portrait only: the page is a handset, and the keypad is drawn under
         the screen rather than beside it. -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>
    <!-- Required since iOS 13, and an app without one is letterboxed rather
         than filling the screen — which would break the layout that measures
         itself against the window. -->
    <key>UILaunchScreen</key>
    <dict>
        <key>UIColorName</key>
        <string></string>
    </dict>
    <key>UIStatusBarHidden</key>
    <true/>
    <!-- The games and saves live in Documents, and these two make that folder
         one the Files app can open. Dropping an archive in from Files is the
         other half of the page's own ＋ 게임 추가 button. -->
    <key>UIFileSharingEnabled</key>
    <true/>
    <key>LSSupportsOpeningDocumentsInPlace</key>
    <true/>
    <!-- The server this app talks to is itself, on loopback. Arbitrary
         cleartext stays off; this is the exception for the one address that is
         never on a network. -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <false/>
        <key>NSAllowsLocalNetworking</key>
        <true/>
        <key>NSExceptionDomains</key>
        <dict>
            <key>127.0.0.1</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <true/>
                <key>NSIncludesSubdomains</key>
                <false/>
            </dict>
            <key>localhost</key>
            <dict>
                <key>NSExceptionAllowsInsecureHTTPLoads</key>
                <true/>
            </dict>
        </dict>
    </dict>
</dict>
</plist>
